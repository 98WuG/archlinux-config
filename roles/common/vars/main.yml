---
conflicting_packages:
  - htop
  - mlocate
  - docker
  - vim

packages:
  - bat
  - docker-compose
  - fzf
  - gvim
  - htim
  - pkgfile
  - plocate
  - podman
  - podman-docker
  - tmux
  - zsh

home: "{{ lookup('env', 'HOME') }}"
